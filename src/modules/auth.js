import { handleActions } from "redux-actions";
// import createAction in redux-actions

const initialState ={
    login: 'false',
    username: 'PaulH',
}


export default handleActions({

}, initialState);